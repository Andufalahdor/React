// test("Should return 2 when add one and one", () => {
//   expect(2 + 0).toBe(2);
// });

describe("Add operator", () => {
  test("Should return 2 when add one and one", () => {
    expect(2 + 0).toBe(2);
  });

  //   describe("Again Add operator", () => {
  //     test("Should return 2 when add one and one", () => {
  //       expect(2 + 0).toBe(2);
  //     });
  //   });
});
